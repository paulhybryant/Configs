# vim: filetype=zsh sw=2 ts=2 sts=2 et tw=80 foldlevel=0 nospell

MOUNTDIR=$(hdiutil mount "$1" | tail -1 | awk '{$1=$2=""; print $0}' | sed 's/ //g')
zsh::eval "sudo installer -pkg "${MOUNTDIR}/"*.pkg -target /"
