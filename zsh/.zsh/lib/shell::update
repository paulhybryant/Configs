# vim: filetype=zsh sw=2 ts=2 sts=2 et tw=80 foldlevel=0 nospell

setopt localoptions err_return nounset

shell::eval "antigen update"
shell::eval "brew update && brew upgrade \\$(brew outdated)"
shell::eval "pip list --outdated | awk '{print \\$1}' | xargs -n 1 pip install -Up"
