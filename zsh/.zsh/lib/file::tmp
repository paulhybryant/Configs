# vim: filetype=zsh sw=2 ts=2 sts=2 et tw=80 foldlevel=0 nospell

setopt localoptions err_return nounset

zparseopts -D -cb:=callback
local _tmp
_tmp=$(mktemp)

tee ${_tmp}

if [[ ${#callback} -ge 2 ]]; then
  zsh::eval "${callback[2]} ${_tmp}"
fi
