#!/usr/bin/env zsh
#
fpath+=($HOME/.zsh/lib)
autoload -Uz os::LINUX os::OSX osx::xclip ssh::xclip

setopt localoptions err_return

if os::LINUX; then
  if [[ "${SSH_OS}" == "Darwin" ]]; then
    ssh::xclip "$@"
  else
    xclip "$@"
  fi
elif os::OSX; then
  osx::xclip "$@"
fi
